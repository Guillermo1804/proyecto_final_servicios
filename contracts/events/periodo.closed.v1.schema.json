{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "periodo.closed.v1 payload",
  "type": "object",
  "required": ["periodo_id", "activo"],
  "properties": {
    "periodo_id": { "type": "integer" },
    "nombre": { "type": "string" },
    "activo": { "type": "boolean" }
  }
}
