{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "user.updated.v1 payload",
  "type": "object",
  "required": ["user_id", "email", "nombre", "rol", "activo"],
  "properties": {
    "user_id": { "type": "integer" },
    "email": { "type": "string", "format": "email" },
    "nombre": { "type": "string" },
    "rol": { "type": "string" },
    "activo": { "type": "boolean" }
  }
}
